"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const compileSolcjs_1 = require("./compileSolcjs");
const compileNative_1 = require("./compileNative");
const compileDocker_1 = require("./compileDocker");
function getCompileFunction(config) {
    if (config.compilerType === 'native') {
        return compileNative_1.compileNative(config);
    }
    else if (config.compilerType === 'dockerized-solc') {
        return compileDocker_1.compileDocker(config);
    }
    else if (config.compilerType === 'solcjs') {
        return compileSolcjs_1.compileSolcjs(config);
    }
    throw new Error(`Unknown compiler ${config.compilerType}`);
}
exports.getCompileFunction = getCompileFunction;
